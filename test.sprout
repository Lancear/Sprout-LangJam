import std.Console as console;
import std.Collections;

fn main() {
    let a = true;
    let b = a ? "true" : "false";
    let c = 1 + 3;
    let d = a && c > 2;
    let e = -2 < 0;
    let f = !a;
    let h = c++;
    let i = --c;
    let j = b + ", yeah!";
    mut x = "hello";
    mut y = x = "hell no";
    mut z = x += " world";

    for (mut x = 0; x < 12; x += 2);
    for (mut x = 0;; x++) return 69;

    while (true != false && !false) {
        return 5 - 4 * 7 / 12 % 0;
    }

    console.log( true ? false : true );
    console.log("arg1", "arg2", 3, true);
}
