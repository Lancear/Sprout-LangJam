import std.Console as console;
import std.Collections;

fn square(x: int): int {
    return x * x;
}

fn main(): int {
    let a = true;
    let b = a ? "true" : "false";
    let c = 1 + 3;
    let d = a && c > 2;
    let e = -2 < 0;
    let f = !a;
    let h = c++;
    let i = --c;
    mut k = "hello";
    mut l = k = "hell no";

    while (true != false && !false) {
        return 5 - 4 * 7 / 12 % 10;
    }

    let x = true;
    mut y = 3;
    mut z = 1;

    while (z <= 3) {
        y = y + z;
        z = z + 1;
    }
    
    return y + square(7);
}
